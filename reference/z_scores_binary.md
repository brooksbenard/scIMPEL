# Z-scores from logistic regression per gene (binary outcome)

Z-scores from logistic regression per gene (binary outcome)

## Usage

``` r
z_scores_binary(
  expr,
  group,
  verbose = TRUE,
  binary_positive_reference = c("second", "first")
)
```
