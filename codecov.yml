# Codecov configuration
# https://docs.codecov.com/docs/codecov-yaml
coverage:
  range: "90...100"
  status:
    project:
      default:
        target: 90%
        threshold: 1%
    patch:
      default:
        target: 90%
