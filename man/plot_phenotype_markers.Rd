% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_phenotype_markers.R
\name{plot_phenotype_markers}
\alias{plot_phenotype_markers}
\title{Plot phenotype marker gene heatmaps (ComplexHeatmap)}
\usage{
plot_phenotype_markers(
  markers,
  expr_mat,
  meta,
  cell_id_col = "Cell",
  group_col,
  score_col,
  celltype_col = "celltype_original",
  celltype_palette = NULL,
  heatmap_type = c("global", "cell_type_specific"),
  top_n_markers = 20L,
  n_mark_labels = 5L,
  p_adj_threshold = 0.05,
  scale_clip = NULL,
  column_title = NULL,
  draw = TRUE,
  use_raster = FALSE
)
}
\arguments{
\item{markers}{List returned by \code{find_phenotype_markers()} with elements
\code{adverse_markers} and \code{favorable_markers}.}

\item{expr_mat}{Numeric matrix, genes \eqn{\times} cells (column names = cell IDs).}

\item{meta}{Data frame of cell metadata with at least \code{cell_id_col},
\code{group_col}, \code{score_col}, and \code{celltype_col}.}

\item{cell_id_col}{Column in \code{meta} with cell IDs matching \code{colnames(expr_mat)}.}

\item{group_col}{Column with phenotype groups (\code{Most Favorable}, \code{Other},
\code{Most Adverse}).}

\item{score_col}{Column with continuous phenotype scores for the top color bar.}

\item{celltype_col}{Column with cell type labels.}

\item{celltype_palette}{Named vector of colors for cell types. If \code{NULL},
\code{\link{get_celltype_palette}()} is used.}

\item{heatmap_type}{\code{"global"} (cell-type agnostic markers) or
\code{"cell_type_specific"} (markers per cell type from
\code{marker_scope = "cell_type_specific"}).}

\item{top_n_markers}{Maximum number of genes to keep per contrast block (per tail
for global; per phenotype bin \eqn{\times} cell type for cell-type-specific).}

\item{n_mark_labels}{Number of row labels to draw per block via
\code{ComplexHeatmap::anno_mark} (top genes by \code{avg_log2FC} within each block).}

\item{p_adj_threshold}{Only genes with \code{p_adj} below this value (and positive
\code{avg_log2FC}) are candidates before ordering by log fold change.}

\item{scale_clip}{Length-2 numeric vector \code{c(lo, hi)} applied after row scaling
(values outside are clipped). If \code{NULL}, uses \code{c(-3, 3)} for
\code{heatmap_type = "global"} and \code{c(-5, 5)} for cell-type-specific.}

\item{column_title}{Optional title above the heatmap.}

\item{draw}{If \code{TRUE} (default), calls \code{ComplexHeatmap::draw()}. If
\code{FALSE}, returns the \code{Heatmap} object invisibly.}

\item{use_raster}{Passed to \code{Heatmap()} (default \code{FALSE}).}
}
\value{
Invisibly, the \code{ComplexHeatmap::Heatmap} object (or \code{NULL} if
nothing was plotted).
}
\description{
Draws a \strong{cell-type-agnostic} (global) or \strong{cell-type-specific} heatmap from
the output of \code{\link{find_phenotype_markers}()}. Expression is subset to
the selected marker genes and ordered cells \strong{before} row-wise scaling, to keep
transpose/scale costs small on large matrices.
}
\details{
Requires suggested packages \strong{ComplexHeatmap} and \strong{circlize}.

For \code{heatmap_type = "global"}, rows are favorable-marker genes then
adverse-only genes (no duplicate genes in the adverse block). For
\code{cell_type_specific}, rows follow phenotype bin then cell type, matching
column order.
}
\seealso{
\code{\link{find_phenotype_markers}()}
}
